fight = (enemyName, enemyHP, enemyLoot) ->
	if atk[1] == "unarmed"
		addText("Do you still want to fight the #{enemyName} even though you do not have a weapon?")
		if fightCheckInput?
			addText("Ok, but you might die quickly if you do not have any potions.")
		else
			break
		
		if 
	else
			
	